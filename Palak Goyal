void reverseString(char* s, int sSize)
{
    int a=0,e=sSize-1,i;
    char temp;
    while(a<e)
    {
        temp=s[a];
        s[a]=s[e];
        s[e]=temp;
        a++;
        e--;
    }
    printf("%s",s);
  
}
